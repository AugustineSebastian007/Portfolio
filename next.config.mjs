/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'github-readme-stats-augustine-sebastians-projects.vercel.app',
      'streak-stats.demolab.com',
      'skillicons.dev'
    ]
  }
};

export default nextConfig;
